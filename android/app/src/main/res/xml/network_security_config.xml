<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release builds only trust HTTPS endpoints. Local HTTP is configured in the debug source set. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">consistencygrid.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
