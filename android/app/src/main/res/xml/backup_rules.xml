<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude all shared preferences -->
    <exclude domain="sharedpref" path="." />
    <!-- Exclude room databases -->
    <exclude domain="database" path="." />
    <!-- Exclude the reel backup directory in filesDir -->
    <exclude domain="file" path="reel_backup" />
</full-backup-content>
